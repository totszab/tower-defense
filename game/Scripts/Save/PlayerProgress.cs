namespace TowerDefense.Save;

// Bootstrap subset only — UnlockedSkillNodeIds, HighestUnlockedLevelIndex és
// LevelPresets a skill fa / szint-progresszió munkával kerülnek be (ROADMAP Fázis 4).
public class PlayerProgress
{
    public int MetaCurrency { get; set; }
}
